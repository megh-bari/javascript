// Immediately Invoked Function Expression

(function code() {
    // named IIFE
  console.log("DB Connected!");
})();

// syntax:
// (function defination )
// (exicution)

((name) => {
  console.log(`${name}, welcome to database`);
})("Megh");
